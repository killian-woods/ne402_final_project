STEPS TO INSTALL:

## 1. Prerequisites
- Python 3.10 or newer installed
- `pip` available
- (Optional) `virtualenv` installed for creating virtual environments

## 2. Virtual Environment
python -m venv .venv

## 3. Install package in editable mode (REQUIRED)
pip install -e .

## 4. Run analysis
Run file src/main.py